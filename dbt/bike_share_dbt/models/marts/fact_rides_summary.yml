version: 2

models:
  - name: fact_rides_summary
    description: "Aggregated ride metrics"

    columns:
      - name: total_rides
        tests:
          - not_null

      - name: ride_date
        tests:
          - not_null
