version: 2

models:
  - name: dds_dim_bike
    description: "Bike dimension table"

    columns:
      - name: bike_sk
        tests:
          - not_null
          - unique

      - name: bike_id
        tests:
          - not_null
          - unique
