name: 'bike_share_dbt'
version: '1.0'
config-version: 2

profile: 'bike_share_dbt'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

models:
  bike_share_dbt:

    rds:
      +schema: RDS
      +tags: ["rds"]

    cds:
      +schema: CDS
      +tags: ["cds"]

    dds:
      +schema: DDS
      +tags: ["dds"]

    ids:
      +schema: IDS
      +tags: ["ids"]